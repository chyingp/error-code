export const addErrorCode = (code, desc) => ({
	type: 'ADD_ERROR_CODE',
	payload: {
		code,
		desc
	}
})

// export const addErrorCode = (code, desc) => ({
// 	type: 'ADD_ERROR_CODE',
// 	payload: {
// 		code,
// 		desc
// 	}
// })